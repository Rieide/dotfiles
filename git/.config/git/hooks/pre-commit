#!/usr/bin/env bash
set -Eeuo pipefail

if ! command -v gitleaks >/dev/null 2>&1; then
  printf 'Gitleaks pre-commit check skipped: gitleaks is missing from PATH\n' >&2
  exit 0
fi

exec gitleaks protect --staged --redact --no-banner --log-level warn
